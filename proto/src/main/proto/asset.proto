syntax = "proto3";

option java_multiple_files = true;
option java_package = "io.metaloom.loom.proto";
option java_outer_classname = "AssetProto";
option objc_class_prefix = "HLW";

package asset;

// The asset loader service definition.
service AssetLoader {
  // Store asset
  rpc Store (AssetRequest) returns (AssetResponse) {}
  
  // Load asset
  rpc Load (AssetRequest) returns (AssetResponse) {}
}

// The request for the asset
message AssetRequest {
  string sha512sum = 1;
  string sha256sum = 2;
  string md5sum = 3;
  string chunkHash = 4;
  string path = 5;
  uint64 size = 6;
  string fingerprint = 7;
  uint64 zeroChunkCount = 8;
}

// The response for the asset
message AssetResponse {
  string uuid = 1;
  string path = 2;
  string initialPath = 3;
  uint64 size = 4;
  string sha512sum = 5;
  string sha256sum = 6;
  string md5sum = 7;
  string fingerprint = 8;
  string chunkHash = 9;
  uint64 zeroChunkCount = 10;
}